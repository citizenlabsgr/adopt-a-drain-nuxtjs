/*
class TableHelper {
  constructor (table_key, component) {
    // component is the nuxt component
    this.component = component
    this.table_key = table_key
  }
  log( msg ) {
     this.component.log(msg)
  }
  add( drain ) {
    this.component.drain_info.push(drain)

    //this.component.drain_info.push(drain)
    // for (key in drain) {
    //  this.log(key + " -> " + drain[key]);
    // }

  }
}

export { TableHelper }
*/